@using AppointmentTracker.Services
@model AppointmentTracker.Models.CustomerModel
@{
    var items = CustomerRepository.Customers.Select(x => new SelectListItem(x.Name, x.Id.ToString()));
}

<select asp-for="Id" asp-items="@items"></select>
